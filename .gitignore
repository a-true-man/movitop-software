# תלויות של Node.js (מותקנות ע"י npm install)
node_modules/

# תוצרי בנייה (React & Electron)
build/
dist/
dist-mac/

# קבצי מערכת
.DS_Store
Thumbs.db

# --- OpenTripPlanner Backend ---
# נתעלם מהקבצים הגדולים, הסקריפט יוריד/ייצור אותם
backend/data/
backend/graph.obj
backend/otp.jar

# --- Java Runtime ---
# לא מעלים את ג'אווה לגיט (זה כבד מאוד). המשתמש יוריד לבד או דרך סקריפט.
jre/

# --- מפות ונתונים ---
# קובץ המפה הוא ענק. עדיף לתת לינק להורדה ב-README או להשתמש ב-Git LFS
public/israel.pmtiles

# קבצי סביבה – רק קבצים שעלולים להכיל סודות
# .env.production מוּעד לפריסה – כולל רק הגדרות build (ללא מפתחות)
.env
.env.local
.env.development.local
.env.test.local
public/*.mbtiles
public/*.pmtiles